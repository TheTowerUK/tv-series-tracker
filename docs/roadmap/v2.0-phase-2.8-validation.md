# TV Series Tracker v2.0 — Phase 2.8 Release Readiness Validation

**Phase:** 2.8 — Release readiness and v2.0 closure

**Status:** In progress — Step 2 complete / project-owner approved

**Last updated:** 2026-08-28

**Authority:** [v2.0 roadmap](v2.0-plan.md), [Phase 2.7 hosted validation](v2.0-phase-2.7-validation.md), and the approved Phase 2.8 release-readiness scope.

Statuses are `Pass`, `Fail`, `Blocked`, or `Pending`. This record tracks release-readiness evidence and does not mark Phase 2.8 or v2.0 complete.

## Step 1 — inspection

| Check | Status | Evidence |
|---|---|---|
| Application and release inspection | Pass | Project-owner approved on 2026-08-28. The inspection identified obsolete root README guidance and production-facing authority/TMDB/backup language as Step 2 release blockers. |
| Phase 2.7 baseline | Pass | Approved closure commit `c8b90d6a487ee2ffbafbcd616ac0747615815e1c` was pushed before Step 2 work; `main` and `origin/main` matched at the implementation baseline. |

## Step 2 — release blockers and user-facing language

| Check | Status | Evidence |
|---|---|---|
| Root README | Pass | Rewritten for v2.0 device use, optional invite-only cloud operation, hosted artwork search, backup/export recovery position, and current deployment/development workflow. Obsolete no-backend and browser TMDB-token instructions were removed. |
| Authority wording | Pass | The account panel consumes the existing tracker-authority state and distinguishes device authority before cutover from verified cloud authority. It states that device data is preserved and returns after sign-out. |
| Invite-only access | Pass | Normal UI explains that private cloud access is invite-only without disclosing whether an email address exists. Signup remains disabled and Auth error normalization is unchanged. |
| Artwork language | Pass | The development-facing artwork-test label and visible TMDB numeric identifier were removed. Internal TMDB identity and the existing selection workflow remain unchanged. |
| Backup and export | Pass | Normal UI distinguishes a device backup from a cloud export and explains that initial v2.0 cloud restore is support-assisted. No restore UI was introduced. |
| Scope boundary | Pass | No database migration/RPC, hosted Auth, Edge Function, Pages, signup, restore, offline queue, Realtime, version, tag, or release change is part of Step 2. |
| Automated and manual validation | Pass | The complete Node suite discovered 177 tests: 167 passed and 10 opt-in integration tests were intentionally skipped. Syntax checks passed for 58 JavaScript files, all relative links across 26 Markdown files resolved, tracked-path/changed-file secret scanning passed, obsolete/internal user-language scanning passed, and `git diff --check` passed. A local signed-out browser smoke check rendered the invite-only and device/cloud backup wording with no console warning or error. Authenticated/cloud wording is covered deterministically against the existing authority integration; no hosted session or data was changed for this copy-only correction. |
| Project-owner approval | Pass | Step 2 was approved on 2026-08-28. Phase 2.8 remains in progress. |

## Deferred release-readiness work

- Accessibility and mobile corrections identified during Step 1 remain outside Step 2.
- Self-service cloud restore remains deferred beyond the initial v2.0 release.
- Release-candidate versioning, tagging, release notes, and final release approval remain future gated work.

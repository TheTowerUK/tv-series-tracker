# TV Series Tracker v2.0 — Phase 2.8 Release Readiness Validation

**Phase:** 2.8 — Release readiness and v2.0 closure

**Status:** Complete — final v2.0.0 release preparation passed / tag pending project-owner approval

**Last updated:** 2026-08-30

**Authority:** [v2.0 roadmap](v2.0-plan.md), [Phase 2.7 hosted validation](v2.0-phase-2.7-validation.md), and the approved Phase 2.8 release-readiness scope.

Statuses are `Pass`, `Fail`, `Blocked`, or `Pending`. This record tracks release-readiness evidence and does not mark Phase 2.8 or v2.0 complete.

## Step 1 — inspection

| Check | Status | Evidence |
|---|---|---|
| Application and release inspection | Pass | Project-owner approved on 2026-08-28. The inspection identified obsolete root README guidance and production-facing authority/TMDB/backup language as Step 2 release blockers. |
| Phase 2.7 baseline | Pass | Approved closure commit `c8b90d6a487ee2ffbafbcd616ac0747615815e1c` was pushed before Step 2 work; `main` and `origin/main` matched at the implementation baseline. |

## Step 2 — release blockers and user-facing language

| Check | Status | Evidence |
|---|---|---|
| Root README | Pass | Rewritten for v2.0 device use, optional invite-only cloud operation, hosted artwork search, backup/export recovery position, and current deployment/development workflow. Obsolete no-backend and browser TMDB-token instructions were removed. |
| Authority wording | Pass | The account panel consumes the existing tracker-authority state and distinguishes device authority before cutover from verified cloud authority. It states that device data is preserved and returns after sign-out. |
| Invite-only access | Pass | Normal UI explains that private cloud access is invite-only without disclosing whether an email address exists. Signup remains disabled and Auth error normalization is unchanged. |
| Artwork language | Pass | The development-facing artwork-test label and visible TMDB numeric identifier were removed. Internal TMDB identity and the existing selection workflow remain unchanged. |
| Backup and export | Pass | Normal UI distinguishes a device backup from a cloud export and explains that initial v2.0 cloud restore is support-assisted. No restore UI was introduced. |
| Scope boundary | Pass | No database migration/RPC, hosted Auth, Edge Function, Pages, signup, restore, offline queue, Realtime, version, tag, or release change is part of Step 2. |
| Automated and manual validation | Pass | The complete Node suite discovered 177 tests: 167 passed and 10 opt-in integration tests were intentionally skipped. Syntax checks passed for 58 JavaScript files, all relative links across 26 Markdown files resolved, tracked-path/changed-file secret scanning passed, obsolete/internal user-language scanning passed, and `git diff --check` passed. A local signed-out browser smoke check rendered the invite-only and device/cloud backup wording with no console warning or error. Authenticated/cloud wording is covered deterministically against the existing authority integration; no hosted session or data was changed for this copy-only correction. |
| Project-owner approval | Pass | Step 2 was approved on 2026-08-28. Phase 2.8 remains in progress. |

## Step 3 — reviewed artwork enrichment

| Check | Status | Evidence |
|---|---|---|
| Step 3A discovery and classification | Pass | Missing-artwork discovery is deterministic and write-free. Existing artwork is excluded, confident matches remain proposals, ambiguous and no-match results remain distinct, service and rate-limit failures are not misclassified, and cancellation/progress are covered by focused tests. |
| Step 3B review experience | Pass | Authenticated users receive a memory-only discovery and review workflow with explicit selection, manual candidate review, cancellation, and rate-limit resume. Review performs no tracker, LocalStorage, sessionStorage, repository, RPC, or cloud mutation. |
| Step 3C controlled application | Pass | Explicitly selected proposals can be applied through the existing device repository or serialized cloud synchronization controller. Application rereads current authoritative state, never overwrites existing artwork, preserves decimal-string revisions, waits for confirmed cloud refreshes, and stops safely on conflict, stale state, uncertainty, failure, or cancellation without automatic retry. |
| Automated validation | Pass | Focused discovery, review, and application tests cover no-write discovery, proposal validation, device application, serialized cloud application, authoritative rereads, existing-artwork protection, conflict/stale-state stops, cancellation/resume, partial failure, and safe diagnostic handling. The complete Node, cloud mutation/conflict, device repository, syntax, secret, terminology, no-write, dependency/safety, Markdown-link, and diff checks passed at the Step 3C commit gate. |
| Project-owner live validation | Pass | Project-owner testing confirmed appropriate artwork identification, successful selected-artwork application, and successful operation of the complete discovery, review, select, apply, and persistence workflow. The ordinary individual-series Edit → artwork search → select → apply workflow also passed regression validation. |
| Runtime configuration boundary | Pass | The local `config/supabase.local.js` used for assisted validation was runtime-only, contained browser-public configuration, remained Git-ignored, and was not staged or committed. |
| Architecture scope | Pass | Step 3 required no database schema, migration, RPC, RLS, hosted Auth, TMDB Edge Function contract, or Pages deployment change. It reuses the existing authenticated TMDB search and ordinary device/cloud show-update paths. |
| Catalogue scope | Pass | Controlled validation was sufficient for the release gate. Artwork was not applied across the full catalogue, and full-catalogue population was not required. |
| Project-owner approval | Pass | Steps 3A, 3B, and 3C were approved on 2026-08-28. Phase 2.8 remains in progress. |

## Step 4 — release documentation and focused UX/accessibility polish

| Check | Status | Evidence |
|---|---|---|
| User and support guidance | Pass | Root guidance and concise v2.0 release notes cover device/private-cloud operation, invite-only access, migration preservation, backup/export recovery expectations, artwork discovery/review, and user-level privacy boundaries. |
| Historical documentation | Pass | Current-status notices point from the historical roadmap and Phase 2.2 environment contract to current validation evidence without rewriting historical decisions. |
| Focus and semantics | Pass | Native controls retain keyboard operation; consistent `:focus-visible` treatment, labelled dialogs, live artwork summaries, and colour-independent selected-candidate cues are covered by focused release-readiness tests. |
| Narrow layout | Pass | Conflict comparisons, artwork actions, candidate cards, account controls, and dialog actions receive focused narrow-width wrapping/stacking without a UI redesign. Desktop browser smoke found no page, header, or account overflow; the controlled browser could not emulate a narrow viewport, so narrow behavior was verified through scoped responsive CSS and regression inspection rather than claimed as a manual mobile render. |
| Automated validation | Pass | The complete Node suite discovered 209 tests: 199 passed and 10 opt-in integration tests were intentionally skipped. Focused artwork passed 30/30; release-readiness language passed 6/6; cloud mutation/conflict passed 41 with 5 opt-in skips; and device repository passed 15 with 1 opt-in skip. Syntax, Markdown-link, tracked credential, visible-language, and `git diff --check` validation passed. |
| Scope boundary | Pass | Step 4 introduces no product feature, matching/application behavior, self-service restore, backend, Auth, Edge Function, Pages, version, tag, or release change. |
| Project-owner approval | Pass | Step 4 was approved on 2026-08-29. Phase 2.8 remains in progress. |

## Step 5 — release-candidate preparation gate

| Check | Status | Evidence |
|---|---|---|
| Five-commit release-set review | Pass | The five approved Phase 2.8 commits form a coherent user-guidance, write-free artwork discovery, artwork review, controlled application, and release-documentation sequence. No temporary, runtime-only, credential, backend, Auth, Edge Function, or Pages configuration change is included. |
| Release validation | Pass | The full opt-in Node/local suite discovered 209 tests: 208 passed and one access-token-specific test was intentionally skipped. Two clean resets replayed all 14 migrations; all 19 pgTAP files and 445 tests passed after each reset; public and `tracker_private` lint passed; the canonical 352-show / 1,028-season baseline, RPC races, and migration concurrency harnesses passed. Syntax, Markdown-link, credential, runtime-file, browser-smoke, Pages-readiness, and `git diff --check` validation also passed. |
| Release-candidate model | Pass | Package metadata and release notes identify `2.0.0-rc.1` as a prerelease. Stable database/API/export contract version `2.0.0`, schema versions, the TMDB Edge Function contract, dependency versions, and general v2.0 product wording remain unchanged. |
| Project-owner approval | Pass | Step 5 and preparation of `2.0.0-rc.1` were approved on 2026-08-29. Tagging and GitHub Release creation remain separately gated; Phase 2.8 and final v2.0 release approval remain in progress. |

## Step 6 — invite-only release-candidate acceptance

Use the live `v2.0.0-rc.1` application with an approved account. Record each result as `PASS`, `ISSUE`, or `BLOCKER`; leave untested items `Pending`. Do not force a migration or conflict, enrich the full catalogue, or use database/admin access.

- [x] Open the live GitHub Pages application and confirm the signed-out device tracker is usable. Result: `PASS`.
- [x] Add and edit one device show. Result: `PASS`.
- [x] Sign in with an approved invite-only account and confirm the device/private-cloud distinction is understandable. Result: `PASS`.
- [ ] If migration review is applicable, inspect the choices without forcing a migration. Result: `Pending` — not exercised during acceptance.
- [x] Confirm the private cloud tracker loads correctly. Result: `PASS`.
- [ ] Add one cloud show. Result: `Pending` — not reported as exercised during acceptance.
- [x] Edit one cloud show. Result: `PASS`.
- [x] Update season progress, reload, and confirm the cloud state persists. Result: `PASS` where exercised; cloud state also persisted across sign-out and sign-in.
- [ ] If a conflict or error occurs naturally, confirm its message and next action are understandable. Result: `Pending` — no natural conflict was reported.
- [x] Search for artwork on one show. Result: `PASS`.
- [x] Run **Find missing artwork**, review the results, and explicitly apply selected artwork. Result: `PASS` — approximately 120 missing-artwork items were identified and applied, and remained available after sign-out and sign-in.
- [ ] Export the cloud tracker. Result: `Pending` — not reported as exercised during acceptance.
- [x] Sign out and confirm the preserved device tracker remains available. Result: `PASS`.

### Acceptance result definitions

- **PASS:** The workflow completes normally and safely with no release-significant problem.
- **ISSUE:** The workflow remains usable and data stays safe, but there is minor wording, cosmetic, or optional usability feedback. An issue does not require `rc.2` by itself.
- **BLOCKER:** Normal invited-user use is prevented, or behavior creates a credible data-loss, privacy, or security risk.

- **Acceptance summary:** `PASS` — normal invited-user device, authentication, cloud, persistence, and artwork workflows passed. No blocker or data-safety issue was encountered.
- **Invited user/project-owner:** Project owner
- **Date:** 2026-08-30
- **PASS / ISSUE / BLOCKER totals:** 9 PASS / 0 ISSUE / 0 BLOCKER; 4 items not exercised
- **Release recommendation:** `v2.0.0-rc.1` accepted; no `rc.2` is required based on acceptance. Final `2.0.0` versioning remains separately gated.

## Step 7 — final v2.0.0 release preparation

| Check | Status | Evidence |
|---|---|---|
| Final prerequisites | Pass | Step 6 acceptance records 9 PASS, 0 ISSUE, 0 BLOCKER, and four explicitly untested items. No release-significant repository issue is open; `v2.0.0-rc.1` remains the accepted candidate and no application, database, Auth, Edge Function, or Pages contract changed after its publication. |
| Final version model | Pass | Package metadata and release notes promote `2.0.0-rc.1` to final `2.0.0`. Stable database/API/export contract version `2.0.0`, schema versions, TMDB Edge Function contract, dependency versions, and general v2.0 product wording remain unchanged. |
| Final validation | Pass | The complete opt-in Node/local, focused artwork, release-readiness, cloud mutation/conflict, repository, migration/checksum, Auth/browser, TMDB, Pages, database, concurrency, syntax, documentation, credential, runtime-file, and diff validation gate passed. Two clean resets replayed all 14 migrations; all 19 pgTAP files and 445 tests passed after each reset; the canonical 352-show / 1,028-season baseline passed after both resets. |
| Release boundary | Pass | Release preparation is complete. The final `v2.0.0` tag and GitHub Release remain separately gated pending project-owner approval after the final Pages deployment is verified. |

## Deferred release-readiness work

- Accessibility and mobile corrections identified during Step 1 remain outside Step 2.
- Self-service cloud restore remains deferred beyond the initial v2.0 release.
- Final `v2.0.0` tagging and GitHub Release publication remain separately gated.
